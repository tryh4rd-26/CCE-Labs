<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:title="Student Information Viewer"
        app:titleTextColor="@android:color/white" />

    <TextView
        android:id="@+id/tvEmptyState"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No Students Registered"
        android:textSize="18sp"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone" />

    <ScrollView
        android:id="@+id/svDataContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="ListView (Names)"
                android:layout_marginTop="16dp" />
            <ListView
                android:id="@+id/lvStudents"
                android:layout_width="match_parent"
                android:layout_height="150dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="GridView (Profiles)"
                android:layout_marginTop="16dp" />
            <GridView
                android:id="@+id/gvStudents"
                android:layout_width="match_parent"
                android:layout_height="150dp"
                android:numColumns="2"
                android:verticalSpacing="8dp"
                android:horizontalSpacing="8dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="TableLayout (Summary)"
                android:layout_marginTop="16dp" />
            <TableLayout
                android:id="@+id/tlStudents"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:stretchColumns="0,1,2"
                android:padding="8dp">
                <TableRow>
                    <TextView android:text="Name" android:textStyle="bold" />
                    <TextView android:text="Dept" android:textStyle="bold" />
                    <TextView android:text="Gender" android:textStyle="bold" />
                </TableRow>
            </TableLayout>

            <Button
                android:id="@+id/btnShowPopup"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Show Popup Menu"
                android:layout_marginTop="16dp" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
